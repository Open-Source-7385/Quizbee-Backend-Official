package pe.edu.upc.quizbee.subscriptions.domain.services;

public interface PlanCommandService {
    // Por ahora los planes se crean manualmente, pero podríamos agregar comandos aquí
    // Long handle(CreatePlanCommand command);
    // Optional<Plan> handle(UpdatePlanCommand command);
}